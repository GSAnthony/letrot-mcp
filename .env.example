# HTTP Basic Auth credentials for the secured Letrot API host (rec2.letrot.com).
# Required — the server throws on startup of any API call if these are unset.
LETROT_BASIC_AUTH_USER=
LETROT_BASIC_AUTH_PASSWORD=

# HTTP port for the Streamable HTTP MCP transport (optional, defaults to 3001).
# PORT=3001
